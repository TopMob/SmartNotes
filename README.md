# SmartNotes 📝

Современное веб‑приложение для заметок, обновленное до React + Vite.

## Что внутри

- **React + Vite** для быстрой разработки.
- **Zustand** для хранения состояния заметок.
- **Tiptap** как rich‑text редактор.
- **Fuse.js** для нечеткого поиска.
- **Tailwind CSS** для UI‑стилей.
- **dnd-kit** для drag‑and‑drop.
- **React Hook Form + Zod** для форм и валидации.
- **Day.js** для дат и форматирования.
- **React Hot Toast** для уведомлений.

## Запуск локально

```bash
npm install
npm run dev
```

Откройте `http://localhost:5173`.

## Деплой на GitHub Pages

Репозиторий настроен на публикацию из папки `docs/` (ветка `main`).

```bash
npm install
npm run build
```

После сборки закоммитьте папку `docs/` и включите Pages в настройках репозитория
для ветки `main` и директории `/docs`.
