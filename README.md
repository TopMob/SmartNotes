# SmartNotes 📝

Современное веб‑приложение для заметок, обновленное до React + Vite.

## Что внутри

- **React + Vite** для быстрой разработки.
- **Zustand** для хранения состояния заметок.
- **Tiptap** как rich‑text редактор.
- **Fuse.js** для нечеткого поиска.
- **Tailwind CSS** для UI‑стилей.
- **dnd-kit** для drag‑and‑drop.
- **React Hook Form + Zod** для форм и валидации.
- **Day.js** для дат и форматирования.
- **React Hot Toast** для уведомлений.

## Запуск локально

```bash
npm install
npm run dev
```

Откройте `http://localhost:5173`.

## Структура

- `src/` — React-код приложения.
- `public/` — статические файлы (favicon, логотип).
- Наследуемые `css/` и `js/` больше не используются — весь UI и состояние в React.

## Деплой на GitHub Pages

Сайт публикуется из папки `docs/`, которую генерирует Vite.

```bash
npm install
npm run build
```

После сборки закоммитьте `docs/` и в настройках Pages выберите ветку `main`
и папку `/docs`.
